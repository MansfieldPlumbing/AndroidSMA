# AndroidSMA projected scripts

This directory mirrors the private AndroidSMA script directory:

```text
/data/user/0/dev.mansfieldplumbing.androidsma/files/PS1
```

Files are sent through `AndroidSmaRepl.ps1`; ADB is not the normal file channel.
Recovered historical files must match their source Git blob before they are placed here.
